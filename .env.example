# Curation runs through the Copilot CLI (`copilot -p ...`), which is a flat
# seat — no provider API keys. Local runs need a one-time interactive
# login (run `copilot`, then `/login`, or `gh auth login`); CI auth wires in with the run seam.
RESEND_API_KEY=
RECIPIENT_EMAIL=
